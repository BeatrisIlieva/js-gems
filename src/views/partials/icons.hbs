<div class="icon-container">
  {{!-- {% if likes_count > 0 %}
        <button class="btn">
            <a class="zoom-text" href="{% url 'display_liked_jewelries' %}">
                <i class="icon-button fa-regular fa-heart"
                ><span class="likes-count-nav">({{ likes_count }})</span></i
                >
            </a>
        </button>
    {% else %} --}}
  {{! <button class="btn">
            <a class="zoom-text" href="{% url 'display_liked_jewelries' %}">
                <i class="icon-button fa-regular fa-heart"></i>
            </a>
        </button>
    {% endif %}  }}
  {{#if bagCount}}
    <a href="/display-bag/{{user._id}}">
      <i class="icon-button fa-solid fa-bag-shopping"><span
          class="cart-count-nav"
        >({{bagCount}})</span></i>
    </a>
  {{else}}
      <a href="/bag/{{user._id}}">
        <i class="icon-button fa-solid fa-bag-shopping"></i>
      </a>
  {{/if}}
      <a href="/orders-history/{{user._id}}">
        <i class="fa-solid fa-clock-rotate-left"></i>
      </a>
  {{#unless isAuthenticated}}
    <a href="/users/login">
      <i class="icon-button fa-regular fa-user"></i>
    </a>
  {{else}}
    <a href="/profiles/edit">
      <i class="icon-button fa-regular fa-user"></i>
    </a>
  {{/unless}}
</div>

{{!-- <div class="icon-container">
<button class="btn">
    <form method="GET">
      <input
        type="checkbox"
        id="search-toggle"
        class="search-toggle"
        value="{{search}}"
        name="search"
        placeholder="Find Jewelry"
      />
      <input
        class="search"
        name="search"
        value="{{search}}"
        placeholder="Find a Jewelry"
        style="text-align: center"
      />
      <label for="search-toggle" class="search-icon">
        <i
          class="button-no-margin-left icon-button fa-solid fa-magnifying-glass"
        ></i>
      </label>
    </form>
</button>
  {{#if isAuthenticated}}
<button class="btn">
          <a href="/display-bag/{{user._id}}">
          {{#if bagCount}}
          <i class="icon-button fa-solid fa-bag-shopping"></i>
      <span class="cart-count-nav">{{bagCount}}</span>
            {{/if}}
</button>
      {{else}}
<button class="btn">
    <a href="/users/login"><i class="icon-button fa-solid fa-bag-shopping"></i></a>
</button>
    {{/if}}
</div> --}}